<h2>All Heroines</h2>

<h3>Search By Superpower</h3>
<%= form_tag(heroines_path, method: "get") do%>
  <%= text_field_tag :search%>
  <%= submit_tag "Search" %>
<% end %>

<ul>
  <% @heroines.each do |heroine| %>
    <% if !@search || heroine.power.name == @search %>
      <li>
        <%= link_to heroine.super_name, heroine_path(heroine) %>
      </li>
      <br>
    <% end %>
  <% end %>
</ul>
